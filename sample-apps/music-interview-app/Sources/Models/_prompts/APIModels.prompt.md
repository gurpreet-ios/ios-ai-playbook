Implement Data Transfer Objects (DTOs) for the Music Interview app's networking layer.
Create `TrackDTO` and `PlaylistDTO` as `Codable`, `Sendable`, and `Identifiable` structs.
`TrackDTO` should mirror the domain model without offline storage properties.
Ensure all structures strictly comply with Swift 6 concurrency rules.
